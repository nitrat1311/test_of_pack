name: test_of_pack
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.3 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.7.0
  # slot_package:
  #     path: lib/slot_package
    # git:
    #   url: https://github.com/nitrat1311/slot_pack.git
    #   ref: football-cripple-2
dev_dependencies:

  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
